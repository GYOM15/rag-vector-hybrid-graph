# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environnements / secrets
.venv/
venv/
.env

# Artefacts de données (régénérés)
data/corpus/*
!data/corpus/.gitkeep
*.faiss
*.chunks.json
*.meta.json

# macOS
.DS_Store

# Sorties générées (régénérées par les évals) ; on garde les jeux fixes + baselines
eval/*.json
!eval/questions.json
!eval/golden_corpus.json
!eval/baselines.json

# Fichiers perso (non versionnés)
slides_prompts.md
youtube_metadata.md

# Terraform (state + secrets + provider cache; keep the .tf files and the example)
infra/.terraform/
infra/.terraform.lock.hcl
infra/terraform.tfvars
*.tfstate
*.tfstate.*
